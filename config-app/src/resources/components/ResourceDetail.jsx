
import React from "react";

const ResourceDetail = (props) => {
    return (
        <div>
            Resource Detail In Component Here {props.resourceId}
        </div>
    );
};

export default ResourceDetail;